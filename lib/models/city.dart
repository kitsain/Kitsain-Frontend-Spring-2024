/// Represents a city with its name and ID.
class City {
  String cityName;
  String cityId;

  /// Constructs a [City] object with the given [cityName] and [cityId].
  City({required this.cityName, required this.cityId});
}
